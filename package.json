{
  "name": "carting-browser-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
